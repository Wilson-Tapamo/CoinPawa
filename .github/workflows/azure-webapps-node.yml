name: Deploiement Node.js vers o2switch

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  NODE_VERSION: '20.x'                

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Installation et Build
      run: |
        npm install
        npm run build --if-present

    - name: Upload artifact pour le déploiement
      uses: actions/upload-artifact@v4
      with:
        name: node-app
        path: . # Ou le dossier de build (ex: ./dist) si vous en avez un

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: node-app

    - name: 🚀 Déploiement via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: 21
        # server-dir: /public_html/ # Décommentez et modifiez si vous devez cibler un dossier spécifique
